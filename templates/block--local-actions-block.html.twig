{% if content %}
  <nav{{ attributes }}>
    {{ title_prefix }}
    {% if configuration.label_display %}
      <h2{{ title_attributes }}>{{ label }}</h2>
    {% endif %}
    {{ title_suffix }}
    {{ content }}
  </nav>
{% endif %}
